FROM osgeo/gdal:ubuntu-small-3.0.3

ARG nrn_pr_dir=

ARG nrn_url=

ENV nrn_pr_dir ${nrn_pr_dir}

ENV nrn_url ${nrn_url}

USER root

COPY . /nrn-app

WORKDIR /nrn-app

RUN chmod u+x scripts/dl-data.sh

RUN ./scripts/dl-data.sh

VOLUME /nrn-app/data/raw
